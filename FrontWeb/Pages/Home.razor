@page "/" 

@using Radzen.Blazor
@using Radzen.Blazor.Rendering

@inject IJSRuntime JSRuntime;

<h5>Welcome to SamaCard</h5>

<div class="container">
    <div class="row">
        <div class="col-sm-12 col-lg-6 offset-lg-3 my-5">
            <RadzenCard class="w-100 mb-4">
                <RadzenCheckBox @bind-Value="@showDataLabels" Name="dataLabels"></RadzenCheckBox>
                <RadzenLabel Text="Show Data Labels" Component="dataLabels" Style="margin-left: 8px; vertical-align: middle;" />
            </RadzenCard>
            <RadzenChart>
                <RadzenDonutSeries Data="@revenue" CategoryProperty="Quarter" ValueProperty="Revenue" TotalAngle="180" StartAngle="180">
                    <ChildContent>
                        <RadzenSeriesDataLabels Visible="@showDataLabels" />
                    </ChildContent>
                    <TitleTemplate>
                        <div class="rz-donut-content">
                            <div>Revenue</div>
                            <div>for 2024</div>
                        </div>
                    </TitleTemplate>
                </RadzenDonutSeries>
            </RadzenChart>
        </div>
    </div>
</div>

@code {
    bool showDataLabels = false;

    class DataItem2
    {
        public string Quarter { get; set; }
        public double Revenue { get; set; }
    }

    DataItem2[] revenue = new DataItem2[] {
        new DataItem2
        {
            Quarter = "Q1",
            Revenue = 30000
        },
        new DataItem2
        {
            Quarter = "Q2",
            Revenue = 40000
        },
        new DataItem2
        {
            Quarter = "Q3",
            Revenue = 50000
        },
        new DataItem2
        {
            Quarter = "Q4",
            Revenue = 80000
        },
    };
}

<RadzenChart>
    <RadzenColumnSeries Data=@revenue2 CategoryProperty="Quarter" ValueProperty="Revenue" />
    <RadzenLegend Visible="false" />
</RadzenChart>

<RadzenButton class="rz-border-radius-6" Click="@ShowAlert" Text="Attention" Icon="account_circle" />

@code {
    class DataItem
    {
        public string Quarter { get; set; }
        public double Revenue { get; set; }
    }
    DataItem[] revenue2 = new DataItem[]
    {
        new DataItem { Quarter = "Q1", Revenue = 234000 },
        new DataItem { Quarter = "Q2", Revenue = 284000 },
        new DataItem { Quarter = "Q3", Revenue = 274000 },
        new DataItem { Quarter = "Q4", Revenue = 294000 }
    };

    async void ShowAlert()
    {
        await JSRuntime.InvokeVoidAsync("confirm", new object?[] { "Testing a Radzen Button" });
    }
}